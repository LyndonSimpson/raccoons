{% schema %}
    {
      "name": "about",
      "settings": [
        {
          "type": "text",
          "id": "home-title",
          "label": "Home title",
          "default": "Lorem ipsum truc machin titre homepage (administrable)"
        },
        {
          "type": "text",
          "id": "home-undertitle",
          "label": "Home undertitle",
          "default": "espace sous-titre en dessous (administrable)"
        }
      ],
      "tag": "section",
      "class": "about-section",
      "limit": 1
    }
{% endschema %}

<h1 class="h1"> {{ 'sections.home-title.home-title' | t }} </h1> <!-- Static content : hardcoded in translations because won't change much for example -->

<h4> {{ section.settings.home-undertitle }} </h4> <!-- dynamic content : is changeable in the bakc office of shopify because will change often for example -->