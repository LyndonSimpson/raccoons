{% schema %}

    {
      "name": "footer",
      "settings": [
        {
          "type": "text",
          "id": "footer_text",
          "label": "Footer Text",
          "default": "© 2024 RACCOONS. All rights reserved."
        }
     ],
     "blocks": [
       {
         "type": "footer",
         "name": "Footer",
         "limit": 1,
         "settings": [
           {
             "type": "text",
             "id": "footer_text",
             "label": "Footer Text",
             "default": "© 2024 RACCOONS. All rights reserved."
           }
          ]
        },
        {
          "name": "Social link",
          "type": "social",
          "settings": [
            {
              "type": "text",
              "id": "text",
              "label": "text"
                },
            {
              "type": "url",
              "id": "url",
              "label": "URL"
            }
            ]
        }
     ],
     "presets": [
        {
          "name": "Default Footer",
          "category": "Footer"
        }
     ]
    }
{% endschema %}

<footer>
    <link rel="stylesheet" href="{{ 'footer.css' | asset_url }}">
    <div class="footer-container">

    <div class="contact-info">
      <h1>Informations contact</h1>
      <p>Lorem ipsum address line here</p>
      <p>546 000 consectetur</p>
      <p>0684457823</p>
    </div>

    <div class="footer-nav-menu">
      <ul>
        {% for product in collections['Hydrogen'].products limit:3 %}
          <a href="{{ product.url }}"> {{ product.title }} </a>
        {% endfor %}
      </ul>
      <a href="#privacy-page">Privacy policy</a>
      <a href="#terms-page">terms of use</a>
    </div>

    <div class="right-side-content-footer">
      <h1>Ouverture prochainement d'une boutique Gallet</h1>
      <p>Lorem ipsum address line here</p>
      <p>546 000 consectetur</p>
      <p>0684457823</p>
    </div>
</footer>

<!-- Old footer for testing - delete when new one is coded (html)

        <div class="footer-info">

            <p>{{ section.settings.footer_text }}</p>
            <p>
                {{ shop.brand.slogan }}
            </p>

            <ul class="social-links">
                {% for block in section.blocks %}
                    {% if block.type == 'social' %}
                        <li>
                            <a href="{{ block.settings.url }}">{{ block.settings.text }}</a>
                        </li>
                    {% endif %}
                {% endfor %}
            </ul>

        </div>
    </div>

    -->