<html>
    <head>
        {{content_for_header}}
        {{ 'style.css' | asset_url | stylesheet_tag }}
    </head>
    <body>

        {% render 'header' %}
        
        {{content_for_layout}}
        <h1>Raccoons - the rest should be brown (this looks red tho)</h1>
    </body>
</html>